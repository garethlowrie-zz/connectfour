import * as React from 'react';
import ReactDOM from 'react-dom';
import Root from 'client/components/Root/container';
import 'client/less/index.less';

ReactDOM.render(<Root />, document.getElementById('root'));
